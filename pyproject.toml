[project]
name = "pbs-mcp"
version = "0.4.0"
description = "MCP server for Proxmox Backup Server (datastore, snapshot, GC, verify, prune)"
readme = "README.md"
license = { text = "GPL-3.0-or-later" }
authors = [{ name = "Ahmet Karaca", email = "ahmet@ahmetkaraca.com" }]
requires-python = ">=3.10"
dependencies = [
    "mcp>=2.0,<3",  # floor is 2.x so deploys converge; mcp_instance.py still runs on 1.x
    "pydantic>=2.0.0",
    "httpx>=0.27.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.urls]
Homepage = "https://github.com/ahmetem/pbs-mcp"
Repository = "https://github.com/ahmetem/pbs-mcp"

[project.scripts]
pbs-mcp = "pbs_mcp.server:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["pbs_mcp"]

[tool.setuptools.packages.find]
include = ["pbs_mcp*"]
